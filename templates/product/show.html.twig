{% extends 'base.html.twig' %}

{% block title %}Ver Producto{% endblock %}

{% block body %}

    <h3> <a href="{{ path('product_index') }}"><< Volver al listado</a><h3>
    <h3>Producto</h3>

    <table class="table">
        <tbody>
             {# <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr> #}
            <tr>
                <th>Nombre</th>
                <td>{{ product.nombre }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ product.getCategoria.getNombre }}</td>
            </tr>
        </tbody>
    </table>
    <br>

    <a href="{{ path('product_edit', {'id': product.id}) }}">Editar</a>
    <br>
    <br>

  

    {{ include('product/_delete_form.html.twig') }}
{% endblock %}
